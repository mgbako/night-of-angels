// Development environment.
// Empty appBaseUrl -> QR URLs use window.location.origin (e.g. http://localhost:4200).
export const environment = {
  production: false,
  appBaseUrl: 'https://nightofangel2026.netlify.app/',
};
