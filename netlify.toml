[build]
  command = "npm run build"
  publish = "dist/night-of-angels/browser"

# Single-page app: let Angular handle routing, fall back to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
